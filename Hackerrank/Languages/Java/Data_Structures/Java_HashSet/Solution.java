        Set<String> leftSet = new HashSet<>();

        for( int i=0; i<t; i++ ) {
            leftSet.add( pair_left[i] + " " + pair_right[i] );
            System.out.println( leftSet.size() );
        }
